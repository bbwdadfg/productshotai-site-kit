apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "productshotai-asset-gateway.fullname" . }}
  labels:
    {{- include "productshotai-asset-gateway.labels" . | nindent 4 }}
data:
  WORKSPACE_NAME: {{ .Values.workspace.name | quote }}
  ASSET_ROOT: {{ .Values.workspace.assetRoot | quote }}
